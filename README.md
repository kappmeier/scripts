# scripts

Some scripts I use.

## Python

Minimum required python version is 3.10.

### Installation

Run the following, ideally in a `pyenv` in the script directory:

```console
pip install -e .
```
